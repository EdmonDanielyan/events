import 'package:flutter/material.dart';

class MessageSearchBtn extends StatelessWidget {
  const MessageSearchBtn({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return IconButton(
      onPressed: () {},
      icon: Icon(
        Icons.search,
        size: 28,
      ),
    );
  }
}
