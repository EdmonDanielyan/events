import 'package:flutter/material.dart';

class ChatInfoHeader extends StatelessWidget {
  const ChatInfoHeader({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
